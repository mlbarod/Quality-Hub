# App 카드 크기와 공통 가독성 검토

## 검토 개요

- 검토 일시: 2026-08-07 KST
- 기준 버전: `761d290`의 커밋하지 않은 UI 변경
- 대상: App 카드 크기, 연결 신호, Main 복귀, 일반 버튼·배지 텍스트
- 데이터 경계: 예시 데이터 기반 로컬 UI 목업. 실제 SSO·DB·Spotfire·사내 LLM과 운영 환경은 미연동
- 독립 검토 판정: `보류`
- 메인 개발자 재검증 판정: `조건부 통과`

독립 검토는 확인된 P0 없이 1366×768과 1440×900 홈을 통과했으나, 실행 시간 제한으로 1920×1080·동작 축소·상세 화면 일부가 `미검증`으로 남아 `보류`했다. 메인 개발자가 별도 Chromium 세션에서 해당 범위를 재검증했다.

## 독립 검토 결과

| ID | 근거 상태 / 우선순위 | 결과 |
| --- | --- | --- |
| `DR-APP-01` | `확인` / P0 없음 | 1366×768·1440×900에서 App 카드 240×160px, Agent 겹침 0, 가로 넘침 없음. 육안상 균형과 잘림 문제 없음 |
| `DR-APP-02` | `확인` / P0 없음 | 유휴 연결 신호 4개 실행, 홈 axe 위반 0건, 콘솔 오류 0건 |
| `DR-APP-03` | `미검증` / P1 | 1920×1080, 동작 축소, 상세 화면 Main 키보드 복귀와 버튼·배지 겹침은 독립 검토 시간 안에 확인하지 못함 |

## 메인 개발자 재검증

| 항목 | 결과 | 근거 |
| --- | --- | --- |
| 카드 크기·배치 | 통과 | 1366×768·1440×900은 240×160px, 1920×1080은 280×164px. 세 화면에서 카드·Agent 겹침 0, 가로 넘침 없음 |
| 유휴 연결 신호 | 통과 | 마우스를 올리지 않은 상태에서 네 신호 모두 `orbit-signal-flow` 4.6초 애니메이션 실행 |
| 동작 축소 | 통과 | `prefers-reduced-motion: reduce`에서 네 신호의 계산된 애니메이션 이름 `none`, 실행 애니메이션 0개 |
| Main 복귀 | 통과 | Report·Rule&SOP·Q&A에서 `Main` 선택 후 `dashboardMode=home`, 홈 표시, 대시보드 숨김, 주소 `#home` 확인 |
| 일반 버튼·배지 | 통과 | 상단 메뉴 16px, Report·Rule 복귀 11px, Q&A Main 16px·배지 13px 확인. 1366×768 Report 화면과 Q&A 흐름에서 가로 넘침 없음 |
| 자동 접근성 | 통과 | 홈 axe 위반 0건 |
| 자동 테스트·빌드 | 통과 | Node 테스트 51개, Vitest 4개, Vite 프로덕션 빌드와 `git diff --check` 통과 |

## 최종 판정과 제한사항

요청한 네 가지 변경은 로컬 목업에서 동작하고 확인된 P0·P1은 없어 `조건부 통과`로 판정한다. 독립 검토가 시간 제한으로 모든 상세 화면을 다시 보지 못한 점과 실제 화면 낭독기, 약 50명 동시 사용, 운영 네트워크·보안·실제 연동은 `미검증`이다. 이번 결과를 운영 품질 완료로 해석하지 않는다.
